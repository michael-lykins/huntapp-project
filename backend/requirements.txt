fastapi>=0.112,<1.0
uvicorn
elasticsearch
elastic-opentelemetry
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-asgi
boto3
python-multipart>=0.0.7          # FastAPI file uploads
Pillow
exifread                  # or 'piexif', either works
numpy
opencv-python-headless
torch                     # cpu ok; big, but works
open-clip-torch           # CLIP embeddings
orjson
rq  
piexif                    # for writing EXIF data
redis>5.0.0
pydantic>=2.7,<3.0
requests>=2.32,<3.0
anthropic>=0.40,<1.0

# --- WebSocket + fast uvicorn loop (NEW) ---
uvicorn[standard]>=0.30,<1.0
websockets>=11.0.3,<12.0
wsproto>=1.2,<2.0