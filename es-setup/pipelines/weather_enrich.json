{
  "description": "Normalize weather docs for Ridgeline",
  "processors": [
    { "set": { "field": "location", "value": "{{lat}},{{lon}}" } },
    { "set": { "field": "source", "value": "open-meteo" } }
  ]
}
